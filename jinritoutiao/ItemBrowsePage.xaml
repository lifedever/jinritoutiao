<Page
    x:Class="jinritoutiao.ItemBrowsePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:jinritoutiao"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Height="100" Background="{ThemeResource AppSolidColorBrush}" >
            <TextBlock Margin="20 20 20 0" Foreground="White" FontSize="14" Name="DateTextBlock"/>
            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                <TextBlock Margin="20 10 20 10" Foreground="White" FontSize="28" Name="TitleTextBlock"/>
            </ScrollViewer>
        </StackPanel>
        <WebView Grid.Row="1" Name="ItemWebView" Margin="0,10,0,0" 
                 NavigationCompleted="ItemWebView_OnNavigationCompleted"
                 NavigationStarting="ItemWebView_OnNavigationStarting"></WebView>
        <ProgressRing Grid.Row="1" Height="100" Background="Transparent" Width="100" Name="ProgressRing"></ProgressRing>
    </Grid>
</Page>
